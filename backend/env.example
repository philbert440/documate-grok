# OpenAI API Key (required for embeddings)
OPENAI_API_KEY=your_openai_api_key_here

# xAI API Key (required for chat completions)
XAI_API_KEY=your_xai_api_key_here

# Server Configuration (optional)
PORT=3000
NODE_ENV=development

# HTTPS Configuration (for production)
# Set to 'true' to enable HTTPS
ENABLE_HTTPS=false

# SSL Certificate paths (required if ENABLE_HTTPS=true)
# These should be absolute paths to your SSL certificate files
SSL_CERT_PATH=/path/to/your/certificate.crt
SSL_KEY_PATH=/path/to/your/private.key

# Alternative: Use Let's Encrypt auto-renewal paths (common on Linux servers)
# For backend.philtompkins.com subdomain:
# SSL_CERT_PATH=/etc/letsencrypt/live/backend.philtompkins.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/backend.philtompkins.com/privkey.pem

# Database Configuration (optional)
# The SQLite database will be created automatically in the data/ directory 